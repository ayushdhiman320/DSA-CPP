// unordered set: same as set but it doesn't store elements in sorted order
//                also upper bound and lower bound are not defined for unordered set


#include<bits/stdc++.h>
using namespace std;

void example_unordered_set(){
    unordered_set<int>us; // declaration of unordered set

}

int main(){
    example_unordered_set();
    return 0;
}